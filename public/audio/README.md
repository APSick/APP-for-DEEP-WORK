# Аудио для плеера

Большие файлы (>100 MB) не хранятся в репозитории. Положи сюда локально файлы `.mp3` / `.m4a` и укажи их имена в `src/components/MusicCard.tsx` в массиве `TRACKS`.

Имена файлов должны совпадать с путями в коде (с учётом пробелов и запятых).
